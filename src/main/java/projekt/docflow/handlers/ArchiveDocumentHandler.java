package projekt.docflow.handlers;

import projekt.core.Handler;
import projekt.docflow.commands.ArchiveDocumentCommand;

public class ArchiveDocumentHandler implements Handler<ArchiveDocumentCommand, String> {
    @Override
    public String handle(ArchiveDocumentCommand createDocumentCommand) {
        return null;
    }
}
